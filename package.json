{
  "name": "scribaverse",
  "version": "1.0.0",
  "scripts": {
    "build": "flutter build web --release",
    "dev": "/home/gitpod/flutter/bin/flutter run -d web-server --web-port 3000"
  },
  "devDependencies": {}
}